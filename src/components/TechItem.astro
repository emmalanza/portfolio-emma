---
interface Props {
    title: string,
    icon: string;
}

const {title, icon } = Astro.props;
---

<div class="flex items-center gap-2">
    <img class="size-5 lg:size-6 block" src={icon} alt={`Icono de ${title}`} />
    <h4 class="whitespace-nowrap">{title}</h4>
</div>
